import { Router, type Request, type Response } from "express";
const funcRouter = Router();


funcRouter.post("/contents" ,(req:Request , res:Response)=>{

})

funcRouter.get("/contents" ,(req:Request , res:Response)=>{})

funcRouter.delete("/content/:id" , (req:Request , res:Response)=>{})



export default funcRouter;
